# JSON map of villa unit -> construction "Time Schedule" feed URL (Apps Script /exec).
# Single-quote the value: this file is *sourced* by the shell (set -a; . ./.env),
# which would otherwise strip the JSON's double quotes and leave {C3:https://…},
# and an unparseable map is read as "no feeds" rather than as an error.
# On Vercel paste the {…} on its own, with no surrounding quotes.
CONSTRUCTION_FEEDS='{"C3":"https://script.google.com/macros/s/…/exec","A2":"https://script.google.com/macros/s/…/exec"}'

# Optional: ONE Apps Script /exec URL for the Laporan Harian tab — the labour
# sheet's web app (deploy apps-script/daily-labour.gs). A single URL, not a JSON
# map: there is one labour workbook, unlike the 19 construction feeds above.
# Unset = serve the snapshot committed in daily-data.mjs.
# DAILY_FEED=https://script.google.com/macros/s/AKfy…/exec

# Optional: staleness threshold in days (default 10) and local server port (default 3000).
# STALE_DAYS=10
# PORT=3000
# CACHE_TTL_MIN=360   # Node server in-memory cache (minutes); Refresh bypasses it
